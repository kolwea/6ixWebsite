version: '3.8'
services:
  app:
    build: 
      context: ./
      target: runner
    container_name: 6ixApp
    volumes:
      - .:/app
    environment:
      NODE_ENV: production
  nginx:
    build: ./nginx
    restart: unless-stopped
    ports:
      - '80:80'
    # volumes:
    #   - ./data:/data
    #   - ./letsencrypt:/etc/letsencrypt
